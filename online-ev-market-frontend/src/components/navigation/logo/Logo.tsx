import * as React from 'react';

import { Box } from '@mui/material';

const Logo = () => (
  <Box sx={{ mt: 0.9 }}>
    <img src="/images/logo.png" alt="Online EV Market" />
  </Box>
);

export default Logo;
